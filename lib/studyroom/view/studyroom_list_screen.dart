import 'package:flutter/material.dart';

class StudyroomTicketList extends StatelessWidget {
  const StudyroomTicketList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
